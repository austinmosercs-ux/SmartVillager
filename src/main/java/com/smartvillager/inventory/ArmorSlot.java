package com.smartvillager.inventory;

/**
 * The four equipment slots in a VillagerBackpack, ordered HEAD → FEET.
 * Ordinal is used as the array index — do not reorder.
 */
public enum ArmorSlot {
    HEAD,
    CHEST,
    LEGS,
    FEET
}
